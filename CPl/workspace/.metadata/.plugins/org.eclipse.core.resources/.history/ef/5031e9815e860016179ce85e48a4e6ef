#include "sstream"
#include "string"
#include "iostream"

int calc(int lhs, int rhs, char op) {
	switch (op) {
		case '+':
			return lhs + rhs;
		case '-':
			return lhs - rhs;
		case '*':
			return lhs * rhs;
		case '/':
			return lhs / rhs;
		case '%':
			return lhs % rhs;
		default:
			break;
	}
	return 0;
}

int calc(std::istream& in){
	int lhs{-1};
	char op {};
	int rhs{-1};
	if(in >> lhs >> op >> rhs){
		return calc(lhs,rhs, op);
	}
	return -1;
}
int main(){
	calc(std::cin);
}